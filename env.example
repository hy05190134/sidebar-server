# 企业微信配置示例文件
# 复制此文件为 .env 并填入实际值
# 或者直接设置环境变量

# 企业ID (CorpID)
# 在企业微信管理后台 -> 我的企业 -> 企业信息 中可以找到
WECOM_CORP_ID=your_corp_id

# 企业密钥 (CorpSecret)
# 在企业微信管理后台 -> 应用管理 -> 自建应用 -> 选择应用 -> Secret
# 注意：Secret 需要妥善保管，不要泄露
WECOM_CORP_SECRET=your_corp_secret

# 应用ID (AgentID)
# 在企业微信管理后台 -> 应用管理 -> 自建应用 -> 选择应用 -> AgentId
WECOM_AGENT_ID=your_agent_id

# 注意：jsapi_ticket 现在会自动获取，无需手动配置
# 系统会自动使用 corp_id 和 corp_secret 获取 access_token，然后获取 jsapi_ticket
# 并自动缓存，在过期前自动刷新

# 会话存档 Secret（可选）
# 如果会话存档使用单独的 Secret，请设置此变量
# 如果不设置，将使用 WECOM_CORP_SECRET
# WECOM_ARCHIVE_SECRET=your_archive_secret

# RSA 私钥路径（必需，用于解密会话存档消息）
# 从企业微信管理后台下载的私钥文件路径
WECOM_RSA_PRIVATE_KEY_PATH=./private_key.pem

# 代理配置（可选）
# WECOM_PROXY=socks5://10.0.0.1:8081
# WECOM_PROXY_PASSWD=user:pass

# 语音识别服务配置（可选）
# VOICE_RECOGNITION_API_URL=https://your-voice-api.com/recognize
# VOICE_RECOGNITION_API_KEY=your-api-key

# 数据库配置（用于 suggestion 关联功能）
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_password
DB_NAME=sidebar_db
DB_SSLMODE=disable

# Suggestion 查询配置
# 查询 suggestion 表时的最大条数，默认 10
SUGGESTION_QUERY_LIMIT=10

# Suggestion 相似度阈值配置
# 余弦相似度阈值（0-100），默认 80，只有相似度达到此值才会关联
SUGGESTION_SIMILARITY_THRESHOLD=80
