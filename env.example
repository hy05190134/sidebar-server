# 企业微信配置示例文件
# 复制此文件为 .env 并填入实际值
# 或者直接设置环境变量

# 企业ID (CorpID)
# 在企业微信管理后台 -> 我的企业 -> 企业信息 中可以找到
WECOM_CORP_ID=your_corp_id

# 企业密钥 (CorpSecret)
# 在企业微信管理后台 -> 应用管理 -> 自建应用 -> 选择应用 -> Secret
# 注意：Secret 需要妥善保管，不要泄露
WECOM_CORP_SECRET=your_corp_secret

# 应用ID (AgentID)
# 在企业微信管理后台 -> 应用管理 -> 自建应用 -> 选择应用 -> AgentId
WECOM_AGENT_ID=your_agent_id

# 注意：jsapi_ticket 现在会自动获取，无需手动配置
# 系统会自动使用 corp_id 和 corp_secret 获取 access_token，然后获取 jsapi_ticket
# 并自动缓存，在过期前自动刷新
